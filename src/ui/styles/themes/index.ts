import { DefaultTheme } from "styled-components";

import * as V from "../tokens/values";

const primary: DefaultTheme = {
  themeKey: "primary",
  margin: V.marginDefault,
  spacing: V.spacingDefault,
  rounded: V.roundedDefault,
  shadowLevel: V.shadowLevelDefault,
  fontFamily: V.fontFamilyDefault,
  fontSize: V.fontSizeDefault,
  fontWeight: V.fontWeightDefault,
  gridSpace: V.gridSpace,
  colors: V.colors
};

export { primary };
